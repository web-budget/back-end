spring:
  datasource:
    url: jdbc:h2:mem:${spring.application.name}_db
    username: sa
    password:

  jpa:
    properties:
      hibernate:
        hibernate.dialect: org.hibernate.dialect.H2Dialect

  h2:
    console:
      enabled: true